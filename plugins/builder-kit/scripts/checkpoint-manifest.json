{
  "_comment": "Shipped DEFAULT gate. Generic and deliberately lenient (test/typecheck are optional so it never blocks a fresh project that has neither yet). /jiffi-init scaffolds docs/checkpoints/checkpoint.json and per-phase manifests that TIGHTEN these — e.g. make tests required once a test script exists, and pin the exact artifacts each phase must produce.",
  "checks": [
    { "id": "prd", "label": "PRD exists", "kind": "mechanical", "type": "file-exists", "path": "docs/prd/prd.md" },
    { "id": "checklist", "label": "Acceptance checklist exists", "kind": "mechanical", "type": "file-exists", "path": "docs/prd/acceptance-checklist.md" },
    { "id": "plan", "label": "Implementation plan exists", "kind": "mechanical", "type": "file-exists", "path": "docs/implementation-plan.md" },
    { "id": "tests", "label": "Test suite passes", "kind": "mechanical", "type": "test-command", "expectExit": 0, "optional": true },
    { "id": "typecheck", "label": "Typecheck passes", "kind": "mechanical", "type": "command", "cmd": "npx --no-install tsc --noEmit", "expectExit": 0, "optional": true },
    { "id": "acs-ticked", "label": "All acceptance criteria ticked", "kind": "mechanical", "type": "checklist-done", "path": "docs/prd/acceptance-checklist.md", "optional": true },
    { "id": "adr-index", "label": "ADR index present", "kind": "mechanical", "type": "file-exists", "path": "docs/adr/README.md", "optional": true },
    { "id": "committed", "label": "Phase work committed & pushed", "kind": "semantic", "type": "advisory", "note": "confirm the phase branch is committed and pushed — nothing here forces it" },
    { "id": "ui-review", "label": "UI reviewed vs the design system", "kind": "semantic", "type": "advisory", "note": "if this phase touched UI, run the ui-review skill before ticking" }
  ]
}
